import 'package:flutter/material.dart';
 
/// Placeholder — full version (screen 14 "Mga Badges" in mockups)
/// built once badge-earning logic exists.
class BadgesScreen extends StatelessWidget {
  const BadgesScreen({super.key});
 
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mga Badge')),
      body: const Center(child: Text('Badges coming soon')),
    );
  }
}
 