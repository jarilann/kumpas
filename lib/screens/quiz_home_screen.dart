import 'package:flutter/material.dart';

/// Placeholder — full version (screen 10 in mockups) built in a later step.
class QuizHomeScreen extends StatelessWidget {
  const QuizHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pagsusulit')),
      body: const Center(child: Text('Quiz section coming soon')),
    );
  }
}